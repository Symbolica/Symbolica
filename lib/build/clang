#!/usr/bin/env sh
set -e

p="$@"
set --

xlinker="$XLINKER"
extension=

for x in $p
do
    case "$x" in
        -o) set -- "$@" -o ; extension=".bc" ;;
        -c) set -- "$@" -c ; xlinker= ;;
        *) set -- "$@" "$x$extension" ; extension= ;;
    esac
done

if [ -z "$NO_LLVM" ];
    then clang-11 -emit-llvm -c "$@";
fi

if [ -n "$CODEGEN" ];
    then exec clang-11 $p $xlinker;
fi
