#!/usr/bin/env sh
set -e

XLINKER="$XLINKER -Xlinker -lc -Xlinker -nostdlib -Xlinker -static" \
exec ~/.symbolica/build/clang -nostdinc -nodefaultlibs --sysroot ~/.symbolica/build/sysroot -O0 -Xclang -disable-O0-optnone -fPIC "$@" -isystem ~/.symbolica/build/sysroot/include
